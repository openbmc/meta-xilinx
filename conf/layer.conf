# We have a conf and classes directory, add to BBPATH
BBPATH .= ":${LAYERDIR}"

BBFILE_COLLECTIONS += "xilinx-layer"
BBFILE_PATTERN_xilinx-layer = ""
LAYERVERSION_xilinx-layer = "1"
LAYERSERIES_COMPAT_xilinx-layer = "sumo"
